[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "troute-ewts"
version = "0.1.0"
description = "EWTS helper package for T-Route"
requires-python = ">=3.6"
